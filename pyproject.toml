[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-wiki-minimal"
version = "0.1.0"
description = "Minimal pre-consolidation wiki for LLM workflows"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
